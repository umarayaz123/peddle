<div class="feeds">
  <div id="first-stream"></div>
  <div class="streaming-div w100 bdr-bot">
  </div>
  <% @feeds.each do |feed| %>
      <div class='streaming-div w100 bdr-bot'>
        <div class='w100 mtop20' style='padding-left:10px;'>
          <div class='post-left-hold'>
            <% unless current_user.profile_image.nil? %>
                <%= image_tag(current_user.profile_image.snap, :height => "103px", :width => "103px") %>
            <% else %>
                <img src="/assets/no-image.jpg" height="103px" width="103px" alt=""/>
            <% end %>
          </div>
          <div class='post-cont-hold'><h1><%= current_user.name ? current_user.name : "User" %></h1>

            <p><%= feed.message %></p>

            <div class='like-hold'></div>
            <div class="like-hold">
              <a class="removeClass" href="javascript:;" feed_id="<%= feed.id %>">Remove</a>
              - <%= duration(Time.now-feed.created_at) %>
            </div>
          </div>
        </div>
      </div>
  <% end %>
</div>